---
import { loadColorThemeOnCookie } from "@/feature/theme"
import { BaseHeaderView } from "./BaseHeader"

const theme = loadColorThemeOnCookie(Astro.cookies)
---

<BaseHeaderView theme={theme}><slot /></BaseHeaderView>
